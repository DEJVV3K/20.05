#include <iostream>
#include <fstream>

using namespace std;

int wej, tot = 0;
	while(scanf("%d", &wej) != EOF)
		printf("%d\n", tot += wej);
		
int main ()
{
	ifstream wejscie;
	ofstream wyjscie;
	
	wejscie.open(wejscie.txt);
	wyjscie.open(wyjscie.json);
	
	int licznik = 0;
	
	if (wejscie.good())
	{
		while(!wejscie.eof())
		{
			for(int i = 0; i < 1000; i++)
			wejscie >> t[i];
		}
		for(int i= 0; i >= licznik; i++)
		{
			if(i == licznik)
			{
				if()
			}
		}
	}
	
	wejscie.close();
	wyjscie.close();
	
	return 0;
}
